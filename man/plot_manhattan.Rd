% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas_plots.R
\name{plot_manhattan}
\alias{plot_manhattan}
\title{Draw a publication-ready Manhattan plot}
\usage{
plot_manhattan(
  gwas,
  cutline = NULL,
  suggestive = NULL,
  palette = c("#2C7FB8", "#7B3294"),
  color_by = c("chromosome", "significance", "pve"),
  point_size_by = c("PVE", "none"),
  point_size = 0.58,
  highlight = NULL,
  highlight_col = "#D73027",
  label_top = 0L,
  show_pve_legend = TRUE,
  title = "Manhattan plot",
  xlab = "Chromosome",
  ylab = expression(-log[10](P)),
  draw = TRUE,
  threshold = NULL,
  threshold_col = NULL,
  threshold_lty = 2,
  threshold_lwd = 1,
  threshold_color = NULL,
  suggestive_col = "#8C8C8C",
  suggestive_lty = 3,
  suggestive_lwd = 0.9,
  suggestive_color = NULL,
  chr_colors = NULL,
  color_cycle = TRUE,
  chr_labels = NULL,
  colors = NULL,
  gap_ratio = 0.05,
  gap_bp = NULL,
  gap_mode = c("fixed", "relative"),
  chr_lengths = NULL,
  width_mode = c("length", "observed", "equal"),
  ylim = NULL,
  xlim = NULL,
  pch = 16,
  point_cex = NULL,
  signal_cex = 1,
  color_significant = FALSE,
  significant_color = "#E64B35",
  nonsignificant_color = "#2C7FB8",
  alpha = 0.82,
  alpha_points = NULL,
  line_width = NULL,
  threshold_labels = NULL,
  suggestive_labels = NULL,
  show_threshold_legend = TRUE,
  show_grid = TRUE,
  grid_color = "#EEF2F5",
  grid_lty = 1,
  grid_lwd = 0.7,
  axis_cex = 0.72,
  label_cex = 0.58,
  title_cex = 1.04,
  label_angle = 35,
  label_offset = 0.025,
  x_axis_las = 1,
  highlight_pch = 21,
  highlight_cex = 1.25,
  legend_cex = 0.62
)
}
\arguments{
\item{gwas}{A GWAS table, a path accepted by [read_gwas()], or the result of
[gwas_mlm()]. Required columns are chromosome, position, and P/value.}

\item{cutline}{A `-log10(P)` threshold. `NULL` uses the Bonferroni threshold
`-log10(0.05/n)`, and `Inf` suppresses the line. Kept for backward
compatibility; use `threshold` for one or more lines.}

\item{suggestive}{Optional vector of suggestive `-log10(P)` thresholds.}

\item{palette}{Fallback chromosome colors. Values are recycled when
`color_cycle = TRUE`.}

\item{color_by}{Point colors: chromosome, significance, or PVE.}

\item{point_size_by}{Encode PVE in point size when available, or use none.}

\item{point_size}{Base point size.}

\item{highlight}{SNP IDs, `chr:position` strings, or a table with ID/position.}

\item{highlight_col}{Color used for highlighted SNPs.}

\item{label_top}{Number of top SNPs to label.}

\item{show_pve_legend}{Add a PVE size legend when PVE is available.}

\item{title, xlab, ylab}{Plot title and axis labels.}

\item{draw}{Draw immediately; otherwise return an object for later printing.}

\item{threshold}{One or more `-log10(P)` thresholds. Overrides `cutline`.}

\item{threshold_col, threshold_color}{Colors for threshold lines; the latter
is a compatibility alias. Values are recycled across thresholds.}

\item{threshold_lty, threshold_lwd}{Line types and widths for threshold lines.}

\item{suggestive_col, suggestive_color}{Colors for suggestive lines.}

\item{suggestive_lty, suggestive_lwd}{Line types and widths for suggestive lines.}

\item{chr_colors}{A named or unnamed vector of per-chromosome colors.
Named colors are matched to chromosome labels.}

\item{color_cycle}{Recycle `chr_colors` or `palette` over chromosomes.}

\item{chr_labels}{Optional named or positional chromosome labels.}

\item{colors}{Alias for `chr_colors`.}

\item{gap_ratio}{Scaling factor used to derive an automatic gap when
`gap_bp` is not supplied. With the default `gap_mode = "fixed"`, the
resulting single gap is used at every chromosome boundary.}

\item{gap_bp}{Explicit fixed gap on the genomic-position scale between
chromosomes. This is in the same coordinate unit as `pos` (for example,
5e6 for a 5-Mb gap).}

\item{gap_mode}{Gap rule: `"fixed"` uses one identical gap at every
chromosome boundary (the publication default); `"relative"` scales each
boundary gap by the width of the preceding chromosome. Supplying `gap_bp`
always selects the fixed rule.}

\item{chr_lengths}{Optional named or positional physical chromosome lengths
in bp. They determine the relative panel widths and must be at least as
large as the observed marker span.}

\item{width_mode}{Width rule when `chr_lengths` is `NULL`: `"length"`
uses the largest observed coordinate, `"observed"` uses the marker span,
and `"equal"` gives every chromosome the same width.}

\item{ylim, xlim}{Optional two-element plotting ranges.}

\item{pch}{Point plotting symbol.}

\item{point_cex}{Alias for `point_size`.}

\item{signal_cex}{Multiplicative size for points at or above the first threshold.}

\item{color_significant}{Recolor significant chromosome points.}

\item{significant_color, nonsignificant_color}{Colors used by significance mode.}

\item{alpha, alpha_points}{Point transparency in `[0, 1]`; the latter is an alias.}

\item{line_width}{Set both threshold and suggestive line widths.}

\item{threshold_labels, suggestive_labels}{Legend labels for the lines.}

\item{show_threshold_legend}{Add a line legend.}

\item{show_grid, grid_color, grid_lty, grid_lwd}{Horizontal grid-line controls.}

\item{axis_cex, label_cex, title_cex}{Font scaling controls.}

\item{label_angle, label_offset}{Controls for top-SNP labels.}

\item{x_axis_las}{Orientation of chromosome-axis labels.}

\item{highlight_pch, highlight_cex}{Symbol and size multiplier for highlights.}

\item{legend_cex}{Legend font scaling.}
}
\value{
An object of class `manhattan_plot`.
}
\description{
Draw a publication-ready Manhattan plot
}
