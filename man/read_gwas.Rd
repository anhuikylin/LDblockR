% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{read_gwas}
\alias{read_gwas}
\title{Read regional association statistics}
\usage{
read_gwas(
  x,
  region = NULL,
  chr_col = NULL,
  pos_col = NULL,
  p_col = NULL,
  id_col = NULL,
  value_is_logp = FALSE,
  header = NULL,
  sep = ""
)
}
\arguments{
\item{x}{File or data frame.}

\item{region}{Optional region.}

\item{chr_col, pos_col, p_col, id_col}{Explicit column names; auto-detected by default.}

\item{value_is_logp}{Whether the selected value is already -log10(P).}

\item{header}{Whether a file has a header; auto-detected by default.}

\item{sep}{File separator; empty means arbitrary whitespace.}
}
\value{
A normalized data frame with `chr`, `pos`, `p`, `logp`, and `id`.
}
\description{
Read regional association statistics
}
\details{
A bare `regional_gwas.tsv` filename resolves to the bundled regional example
after installation. Paths that include a directory are treated as user-supplied
paths. EMMAX `.ps` files and common GAPIT, GEMMA, PLINK/PLINK2, and TASSEL column
names are recognized automatically. When chromosome and position columns are
absent, coordinates are recovered from common marker-ID forms such as
`chr1.S_3279`, `chr1:3279`, and `1_3279`.
}
