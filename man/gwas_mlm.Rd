% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas.R
\name{gwas_mlm}
\alias{gwas_mlm}
\title{Fast GAPIT-principle mixed linear model GWAS}
\usage{
gwas_mlm(
  genotype,
  phenotype,
  trait = NULL,
  kinship = NULL,
  covariates = NULL,
  n_pc = 0L,
  map = NULL,
  sample_ids = NULL,
  replicate = c("expand", "mean"),
  min_maf = 0.05,
  max_missing = 0.2,
  chunk_size = 256L,
  max_iter = 60L,
  verbose = TRUE
)
}
\arguments{
\item{genotype}{An `ld_data` object, a 0/1/2 matrix, a HapMap file, or an
RDS file containing one of those objects.}

\item{phenotype}{A phenotype data frame, a TASSEL phenotype file, or a
numeric vector with names matching genotype samples.}

\item{trait}{Trait column name. The first numeric phenotype column is used
when omitted.}

\item{kinship}{Optional numeric kinship matrix or TASSEL kinship file. If
omitted, a standardized marker relationship is computed once.}

\item{covariates}{Fixed-effect columns, a matrix/data frame, or `NULL` to
use `location`, `Q1`, `Q2`, and `Q3` when present.}

\item{n_pc}{Number of genotype PCs to append to the fixed effects.}

\item{map}{Optional marker map when `genotype` is a matrix.}

\item{sample_ids}{Optional sample IDs when matrix row names are absent.}

\item{replicate}{How repeated phenotype rows are handled: `"expand"`
keeps location replicates, while `"mean"` averages them by taxon.}

\item{min_maf}{Minimum minor allele frequency.}

\item{max_missing}{Maximum marker missing-call fraction.}

\item{chunk_size}{Number of markers processed per BLAS chunk.}

\item{max_iter}{Reserved compatibility argument for variance estimation.}

\item{verbose}{Print model and progress messages.}
}
\value{
A data frame of association statistics with class
  `gwas_mlm_result`; model details are stored in `attr(x, "model")`.
}
\description{
Fits the model `y = X beta + Zu + e`, with `u ~ N(0, K sigma_g^2)`.
The null variance ratio is estimated once (P3D), followed by a vectorized
score/GLS scan in marker chunks. It is the fixed-variance single-marker MLM
test used by the P3D family while avoiding a new REML optimization for every
marker. `PVE` is the marker-specific partial phenotypic variance
explained; `model_PVE` is the null-model variance component estimate.
}
