% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas.R
\name{gwas_ld_region}
\alias{gwas_ld_region}
\title{Select a regional LD window from threshold-exceeding GWAS markers}
\usage{
gwas_ld_region(
  gwas,
  cutline = NULL,
  flank = 250000,
  min_width = 0,
  max_width = Inf,
  chromosome = NULL
)
}
\arguments{
\item{gwas}{A table or file accepted by [read_gwas()].}

\item{cutline}{Threshold on `-log10(P)`. If `NULL`, use the Bonferroni
threshold `-log10(0.05/nrow(gwas))`.}

\item{flank}{Number of bases added on each side of the selected hits.}

\item{min_width}{Minimum interval width in bases.}

\item{max_width}{Optional maximum interval width; it must still contain all
threshold-exceeding hits on the selected chromosome.}

\item{chromosome}{Optional chromosome to force.}
}
\value{
A list of class `gwas_region` with `region`, `chr`, `start`,
  `end`, `lead_id`, `hit_ids`, `cutline`, and the selected GWAS table.
}
\description{
The selected locus is restricted to one local cluster on one chromosome
(the cluster with the strongest peak, then the most hits) so that the
returned interval can be passed directly to `read_hapmap()`,
`read_vcf_region()`, or `read_plink()`. If no marker reaches the threshold,
the most significant marker is used as a reproducible fallback and
`threshold_hit` is set to `FALSE` in the returned table.
}
